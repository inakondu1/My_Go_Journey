package main

import (
	"fmt"
	"strings"
)

func main() {

	text := [10]string{"is", "an", "to", "if", "in", "so", "on", "or", "it", "up"}

	for i := 1; i < 2; i++ {

		text[7] = strings.ToUpper(text[7])
		text[8] = strings.ToUpper(text[8])
		text[9] = strings.ToUpper(text[9])


		fmt.Println(text)
	}
}
